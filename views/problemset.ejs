<!doctype html>
<html>
	<head>
		<meta charset="utf-8">
	</head>
	<body>
		<h1>Мои заметки</h1>
		<ul>
			<% for(let i = 0; i < rows.length; i++){%>
			<li> <%= rows[i].id %>  <%= rows[i].заметка %></li>
			<% } %>
		</ul>
	</body>
</html>